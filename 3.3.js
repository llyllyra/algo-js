// Write a program that will create a duplicate of a given array.

numbers = [1, 2, 3];
numbersCopy = [];
numbers.forEach((number) => {
    numbersCopy.push(number)
})
/*
for (let elem of numbers)
{
    numbersCopy.push(elem)
}

 */
console.log(numbersCopy)

